import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonContainer: {
        marginBottom: 9,
    },
    textButtonContainer: {
        width: '100%',
        alignItems: 'flex-end',
    },
    textButton: {
        fontFamily: 'Intre',
        color: '#3181FF'
    }
});
