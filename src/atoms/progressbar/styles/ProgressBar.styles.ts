import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        borderRadius: 100,
        overflow: 'hidden',
        margin: 20
    },
    fill: {
        height: '100%',
        borderRadius: 100,
    },
});

export default styles;