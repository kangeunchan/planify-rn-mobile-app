import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    title: {
        marginTop: 10,
        fontSize: 20,
        fontFamily: 'Fredoka',
        fontWeight: 'bold',
        color: '#3A86FF'
    },
});
